#!/bin/bash
# Codex launcher - only runs when work mode is inactive

WORK_FILE="$HOME/.config/work/active"

if [ -f "$WORK_FILE" ]; then
    echo "Codex is not available when work mode is active"
    exit 1
else
    exec /home/henry/.bun/bin/codex "$@"
fi
